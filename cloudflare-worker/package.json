{
    "name": "trmnl-sports-worker",
    "version": "1.0.0",
    "description": "Cloudflare Worker backend for the TRMNL Sports plugin. Proxies TheSportsDB v2 API for team search and next-game lookups.",
    "main": "index.js",
    "scripts": {
        "deploy": "wrangler deploy",
        "tail": "wrangler tail",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "keywords": [
        "trmnl",
        "cloudflare-workers",
        "thesportsdb",
        "sports"
    ],
    "author": "Carter Pape",
    "license": "GPL-3.0-or-later",
    "repository": {
        "type": "git",
        "url": "https://github.com/CarterPape/trmnl-sports.git",
        "directory": "cloudflare-worker"
    },
    "type": "module",
    "dependencies": {
        "upng-js": "^2.1.0"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.20.2",
        "@vitest/coverage-istanbul": "^4.1.10",
        "vitest": "^4.1.10"
    },
    "_comment_overrides": "miniflare (via @cloudflare/vitest-pool-workers) exact-pins undici 7.28.0, the top of the GHSA advisory range. 7.29.0 is the patched release in the same major. Test-only; drop this once miniflare ships a fixed pin.",
    "overrides": {
        "undici": "^7.29.0"
    }
}
